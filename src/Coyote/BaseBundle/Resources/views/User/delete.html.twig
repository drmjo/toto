{% extends "CoyoteBaseBundle::layout.html.twig" %}

{% block title %}Are you sure?{% endblock %}

{% block body -%}

<h1></h1>

<div class="row">
	<div class="col-xs-12">
		
		<div class="panel panel-danger">
			<div class="panel-heading">
				<h3 class="panel-title">Delete User</h3>
			</div>
			<div class="panel-body">
			<h4>Warning! This action cannot be undone!</h4>
			{{ form_start(form) }}
			
			{{ form_widget(form.submit, {'attr': {'class': 'btn btn-danger'}}) }}
			<a href="{{ path('coyote_user_show', {'id': user.id }) }}">Cancel</a>

			{{ form_end(form) }}


			</div>
		</div>
	</div>
</div>
{% endblock %}

